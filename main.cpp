#include <iostream>
#include <string>

void proccess(const std::string& input)
{
    std::string result = "";
    int start = 0, end = 0;
    int i = 0;
    while (i < input.length()-1) // Until the penultimate element
    {
        //smth 
    }

}
